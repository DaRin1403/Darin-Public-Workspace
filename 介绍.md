# 介绍 · Kaguya 液态玻璃皮肤

> **一句话**：给 DeepSeek Harness Web 换上一身「樱花粉 × 液态玻璃 × 三态一致交互」的二次元皮肤。

## 这是什么

**anime-theme**（Kaguya）是 DeepSeek Harness Web 的浅色模式主题皮肤：

- **视觉**：整页樱花粉世界观——全幅背景图 + 玻璃后立绘 + 左侧悬浮玻璃窗格（底部常驻 Kaguya 立绘）；
- **材质**：所有面板、卡片、按钮统一「液态玻璃」四件套——半透明渐变 + backdrop 模糊 + 白色亮边 + 上高光/下内影/外投影；
- **交互**：A/B/C/D 四类按钮三态（静止 → 悬停加深 → 按下内凹），菜单条目统一模板 D（悬停艳粉白字），全局同一套「手语」；
- **克制**：官方 13 面主题墙只刷 3 面，深色模式 100% 官方原样；红/绿/黄/蓝语义色永不粉化；升级切换零副作用。

## 覆盖范围

对话主界面、输入区（含菜单/计量胶囊）、左边栏（含收起模式六键）、顶部标题栏、后台任务菜单、工具提示、网页搜索卡、工具卡 IO 框、设置全套（模型页/预设页/插件配置卡/插件列表）、轨迹区与轨迹详情、命令面板、动态插件面板。

## 工程亮点

- **变量账本**：一套 `--kaguya-*` 变量（base/soft/rail/hover/press/title/body），改色只动一处；
- **层级梯度铁律**：常驻 < 悬停 < 按下，按压永远更深；
- **语义隔离**：蓝发送键、红危险、绿运行、黄警告保持官方语义；
- **素材零外链**：背景/立绘压缩为 webp 并 base64 内嵌，单图 <1MB，无网络依赖；
- **文档闭环**：设计规范（规则手册）、设计语言、交互模板、交互架构方案四份文档随包分发。

## 适用

- 平台：DeepSeek Harness Web（浏览器端）
- 模式：浅色（深色 = 官方原样）
- 安装：把 `anime-theme` 放进 profile 的 `node_modules`，在 `cordis.patch.yml` 加一行挂载（详见 `安装说明.md`）

## 版本

- **v1.1.0（2026-08-20）**：设置区全覆盖、工具提示深灰字、网页卡/工具卡蓝色液态玻璃、轨迹详情悬停白字、「跳到最新」粉玻璃按钮、占位符浅灰等交互定稿。
- **v1.0.0（2026-08-18）**：首个定稿版——悬浮玻璃侧栏 + 立绘、双层壁纸、A/B/C/D 四类按钮三态、轨迹界面完整设计语言。
