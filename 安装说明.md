# anime-theme v1.1.0 · Kaguya 液态玻璃皮肤

DeepSeek Harness Web 的二次元（Kaguya）液态玻璃皮肤，浅色模式专属。

## 一、这是什么

- **设计语言**：樱花粉 × 液态玻璃 × 三态一致交互（详见 `anime-theme/设计语言.md`）
- **工程规范**：13 面墙只刷 3 面 + 一层 GLASS_CSS，深色模式 100% 官方原样（详见 `anime-theme/设计规范.md`）
- **交互模板**：A/B/C/D 按钮 + 菜单条目模板 D + 变量账本（详见 `anime-theme/交互模板.md`）
- **素材**：背景图 / 立绘 / 侧栏立绘 webp+base64 内嵌进 `lib/client.js`，无外链

## 二、目录结构

```
anime-theme-1.0.0\
├── anime-theme\            ← 插件包（浏览器半边皮肤本体）
│   ├── package.json        ← 身份证（v1.0.0；exports 须含 ./package.json）
│   ├── lib\index.js        ← 后端空壳
│   ├── lib\client.js       ← 皮肤本体（墙 + GLASS_CSS + 内嵌图片）
│   ├── scripts\build-skin.cjs ← 图片压缩内嵌脚本
│   ├── 设计规范.md          ← 颜色/交互定稿表（改动前先查）
│   └── 设计语言.md          ← 设计语言总结
├── cordis.patch.yml        ← 挂载行（一行把皮肤装进网页插件清单）
└── 源图\                   ← 4 张源图（换图后重新构建用）
```

## 三、安装（本机 / 新机 / 恢复）

1. 把 `anime-theme` 文件夹复制到：`C:\Users\<用户名>\.dsh\profiles\web\node_modules\anime-theme\`
2. 把 `cordis.patch.yml` 的挂载行合并进 `C:\Users\<用户名>\.dsh\profiles\web\cordis.patch.yml`（若该文件不存在则直接复制过去）：
   ```yaml
   - insert:
       - id: ui-anime-theme
         name: 'anime-theme'
   ```
3. 重启 `dsh web`（新增挂载行需要重启；之后改颜色只需 F5）

## 四、日常改动工作流

- 改颜色/位置：编辑 `lib\client.js` → 保存 → 浏览器 **F5**（无需重启）
- 换图：替换 `源图\` 里同名文件（文件名写死）→ 在 `profiles\web` 下运行
  `node node_modules\anime-theme\scripts\build-skin.cjs` → F5
- 改坏了：`lib\client.js.bak-hover-bump`、`lib\client.js.snap-20260818-layered`
  是历史快照，复制回来即可回滚

## 五、版本历史

- **v1.0.0（2026-08-18）**：首个定稿版。包含：悬浮玻璃侧栏+立绘、双层壁纸、
  A/B/C/D 四类按钮三态、收起模式六键统一、轨迹界面完整设计语言（层叠标题块 +
  终端卡代码区）、输入框占位符改写、全局圆角兜底。
